{
  "name": "staff-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api": "npm run dev --prefix apps/api",
    "mobile": "npm run start --prefix apps/mobile",
    "admin": "npm run dev --prefix apps/admin"
  }
}
