{
  "name": "staff-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "export PATH=/opt/alt/alt-nodejs22/root/usr/bin:$PATH && npm install --prefix apps/api && npx tsc --project apps/api/tsconfig.json",
    "start": "cd apps/api && node dist/index.js",
    "api": "npm run dev --prefix apps/api",
    "mobile": "npm run start --prefix apps/mobile"
  }
}
